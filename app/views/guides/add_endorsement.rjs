endorsement = Endorsement.new(params[:endorsement])
page.insert_html :bottom, 'endorsements', :partial => 'endorsement', :locals => {:endorsement => endorsement}
page.insert_html :bottom, 'guide_form', hidden_field_tag("endorsements[#{params[:endorsement_number]}][contest]", endorsement.contest)
page.insert_html :bottom, 'guide_form', hidden_field_tag("endorsements[#{params[:endorsement_number]}][candidate]", endorsement.candidate)
page.insert_html :bottom, 'guide_form', hidden_field_tag("endorsements[#{params[:endorsement_number]}][description]", endorsement.description)
page.insert_html :bottom, 'guide_form', hidden_field_tag("endorsements[#{params[:endorsement_number]}][position_id]", endorsement.position_id)
page.replace_html 'endorsement_number_container', hidden_field_tag(:endorsement_number, params[:endorsement_number].to_i + 1)
